# Course Alignment Matrix

| Course Topic | Course Intent / What it means | CampusEnroll HA Coverage | Evidence in Project | Status | Gap / Recommendation |
| --- | --- | --- | --- | --- | --- |
| Database as Code | Keep schema and seed logic under version control so the database is reproducible and reviewable. | The repo has authoritative SQL in `db/`, but runtime services still mix explicit SQL with Hibernate DDL behavior and there are no migrations yet. | `db/schema.sql`; `db/data.sql`; `db/README.md`; `backend/course-service/src/main/resources/application.yml`; `backend/enrollment-service/src/main/resources/application.yml`; `backend/student-service/src/main/resources/application.yml` | Partially Implemented | Keep `db/` as the course review source and later align services behind migrations instead of mixed `ddl-auto` behavior. |
| Polystore architecture | Use more than one data technology where each one has a clear role. | PostgreSQL is the system of record, Redis is a real cache complement, and RabbitMQ is used for async event flow, but there is no broader multi-database persistence strategy. | `docker-compose.yml`; `backend/course-service/src/main/java/com/campusenroll/courseservice/config/CacheConfiguration.java`; `backend/enrollment-service/src/main/java/com/campusenroll/enrollmentservice/messaging/RabbitConfiguration.java`; `backend/notification/src/main/java/com/campusenroll/notification/messaging/RabbitConfiguration.java` | Partially Implemented | Present this as PostgreSQL plus complementary cache and messaging, not as a full polyglot persistence platform. |
| Distributed architecture | Split responsibilities across bounded services and supporting infra. | Five Spring Boot services, Compose layers, and service-specific docs are present. | `backend/student-service`; `backend/course-service`; `backend/enrollment-service`; `backend/billing-service`; `backend/notification`; `docker-compose.apps.yml` | Implemented | Keep the explanation focused on service responsibilities and the shared academic database constraint. |
| PostgreSQL centralized model | Use PostgreSQL as the main transactional engine for the core domain. | The project intentionally keeps one centralized PostgreSQL database for the course deliverable. | `README.md`; `db/schema.sql`; `.env`; `docker-compose.yml` | Implemented | State clearly that this is a deliberate academic choice aligned with the course scope. |
| Relational integrity | Enforce keys, references, and domain rules at the relational layer. | Students, courses, periods, sections, enrollments, and billings are protected by PKs, FKs, checks, and uniqueness constraints. | `db/schema.sql` | Implemented | Keep using `db/schema.sql` as the authoritative relational proof during presentation. |
| MVCC / ACID | Explain how PostgreSQL guarantees transactional consistency under concurrency. | PostgreSQL provides the runtime engine semantics, and service writes are transactional, but CampusEnroll does not include a dedicated MVCC demonstration of its own flows. | `backend/student-service/src/main/java/com/campusenroll/studentservice/student/StudentService.java`; `backend/enrollment-service/src/main/java/com/campusenroll/enrollmentservice/enrollment/EnrollmentService.java`; `backend/billing-service/src/main/java/com/campusenroll/billing/billing/BillingService.java` | Partially Implemented | Add presentation-ready explanation linking PostgreSQL semantics to current service transactions without claiming a custom MVCC lab inside this repo. |
| Transactions | Group write operations atomically at the service layer. | CRUD and status updates use Spring transactions in the implemented services. | `backend/student-service/src/main/java/com/campusenroll/studentservice/student/StudentService.java`; `backend/course-service/src/main/java/com/campusenroll/courseservice/catalog/service/CourseService.java`; `backend/course-service/src/main/java/com/campusenroll/courseservice/catalog/service/SectionService.java`; `backend/enrollment-service/src/main/java/com/campusenroll/enrollmentservice/enrollment/EnrollmentService.java`; `backend/billing-service/src/main/java/com/campusenroll/billing/billing/BillingService.java` | Implemented | During presentation, connect `@Transactional` behavior to the centralized PostgreSQL model. |
| Isolation levels | Control what concurrent transactions can see and when. | The project relies on PostgreSQL transactional defaults and Spring transactions, but it does not expose explicit per-use-case isolation settings or a repo-local two-session demo. | `backend/enrollment-service/src/main/java/com/campusenroll/enrollmentservice/enrollment/EnrollmentService.java`; `backend/billing-service/src/main/java/com/campusenroll/billing/billing/BillingService.java` | Partially Implemented | Explain default PostgreSQL behavior honestly and avoid claiming a custom isolation-level lab unless one is added later. |
| Locks | Use explicit locking only when the business problem requires it. | Current concurrency protection relies on constraints and flush timing rather than explicit lock statements. | `backend/enrollment-service/src/main/java/com/campusenroll/enrollmentservice/enrollment/EnrollmentService.java`; `db/schema.sql` | Future Production Upgrade | Add explicit locking only if a future workflow truly needs seat reservation or competing inventory claims. |
| Deadlocks | Recognize and handle conflicting concurrent write paths. | No deadlock harness, retry policy, or deadlock-specific demo was found in the repo. | `infra/k6/concurrent-enrollment-test.js`; `backend/enrollment-service/README.md` | Future Production Upgrade | A future concurrency hardening pass could add a deadlock example or retry guidance, but it is not needed for the current academic scope. |
| SKIP LOCKED | Build queue-like or reservation workflows that avoid blocking workers. | No `SKIP LOCKED` usage is present in SQL or application code. | `db/schema.sql`; `backend/` | Future Production Upgrade | Only add this if the project grows into reservation queues or worker-driven seat allocation. |
| Anti-oversell | Prevent multiple successful claims for the same scarce resource. | Duplicate active enrollments are blocked by application checks plus a PostgreSQL partial unique index, but there is no seat-capacity decrement or reservation queue. | `db/schema.sql`; `backend/enrollment-service/src/main/java/com/campusenroll/enrollmentservice/enrollment/EnrollmentService.java`; `infra/k6/concurrent-enrollment-test.js` | Partially Implemented | Present this as duplicate-active protection, not as full seat-inventory anti-oversell. |
| Idempotency | Make repeated requests or retries safe. | `POST /api/enrollments` and `POST /api/billings` now support `Idempotency-Key`, replay successful same-key requests, and reject same-key different-payload reuse with `409 Conflict`. Relational uniqueness still remains as a fallback. | `db/schema.sql`; `backend/enrollment-service/src/main/java/com/campusenroll/enrollmentservice/enrollment/EnrollmentController.java`; `backend/enrollment-service/src/main/java/com/campusenroll/enrollmentservice/idempotency/IdempotencyService.java`; `backend/billing-service/src/main/java/com/campusenroll/billing/billing/BillingController.java`; `backend/billing-service/src/main/java/com/campusenroll/billing/idempotency/IdempotencyService.java`; `docs/course-audit/TRANSACTIONS_CONCURRENCY_IDEMPOTENCY.md` | Partially Implemented | Present this as selected critical-endpoint idempotency. Broader endpoint coverage still remains future work. |
| Transactional outbox | Persist the event intent in the same database transaction as the business change, then publish asynchronously. | Enrollment creation and billing status changes now write `outbox_events` rows in PostgreSQL before RabbitMQ delivery, and scheduled publishers mark rows as `PUBLISHED` or `FAILED`. | `db/schema.sql`; `backend/enrollment-service/src/main/java/com/campusenroll/enrollmentservice/outbox/EnrollmentOutboxService.java`; `backend/enrollment-service/src/main/java/com/campusenroll/enrollmentservice/outbox/EnrollmentOutboxPublisher.java`; `backend/billing-service/src/main/java/com/campusenroll/billing/outbox/BillingOutboxService.java`; `backend/billing-service/src/main/java/com/campusenroll/billing/outbox/BillingOutboxPublisher.java` | Implemented | Present this as improved delivery reliability, not as exactly-once messaging or full distributed compensation. |
| Sagas | Coordinate multi-step distributed business workflows with compensation. | The repo now includes a transactional outbox, but saga compensation, distributed rollback logic, retry choreography, and DLQ behavior are still out of scope. | `backend/enrollment-service/src/main/java/com/campusenroll/enrollmentservice/outbox/EnrollmentOutboxService.java`; `backend/billing-service/src/main/java/com/campusenroll/billing/outbox/BillingOutboxService.java`; `backend/notification/README.md` | Partially Implemented | Keep saga compensation as a future roadmap item and do not present the outbox as full saga completion. |
| Coreography | Let services react to domain events without a central orchestrator. | Enrollment and billing now publish through a transactional outbox to a shared exchange, and notification consumes the resulting events, but the flow is still simple evidence-oriented choreography rather than a full business saga. | `backend/enrollment-service/src/main/java/com/campusenroll/enrollmentservice/outbox/EnrollmentOutboxService.java`; `backend/billing-service/src/main/java/com/campusenroll/billing/outbox/BillingOutboxService.java`; `backend/notification/src/main/java/com/campusenroll/notification/messaging/NotificationEventListener.java`; `backend/notification/src/main/java/com/campusenroll/notification/messaging/RabbitConfiguration.java` | Partially Implemented | Describe this as lightweight event choreography with outbox-backed publication and explicitly distinguish it from orchestration and compensation-heavy saga flows. |
| RabbitMQ messaging | Publish and consume asynchronous events. | RabbitMQ is wired into enrollment, billing, and notification with a real exchange, routing keys, queue bindings, listeners, and now outbox-backed producer publication. | `docker-compose.yml`; `backend/enrollment-service/src/main/resources/application.yml`; `backend/billing-service/src/main/resources/application.yml`; `backend/notification/src/main/resources/application.yml`; `backend/notification/src/main/java/com/campusenroll/notification/messaging/RabbitConfiguration.java`; `db/schema.sql` | Implemented | Demo the exchange, queue, outbox rows, and logs, but avoid claiming exactly-once delivery or full saga recovery. |
| Redis cache | Use Redis to offload repeatable reads or short-lived data. | `course-service` caches list endpoints in Redis and falls back to DB reads if Redis is unavailable. | `backend/course-service/src/main/java/com/campusenroll/courseservice/config/CacheConfiguration.java`; `backend/course-service/src/main/java/com/campusenroll/courseservice/catalog/service/CourseService.java`; `backend/course-service/src/main/java/com/campusenroll/courseservice/catalog/service/AcademicPeriodService.java`; `backend/course-service/src/main/java/com/campusenroll/courseservice/catalog/service/SectionService.java`; `infra/k6/container-failure-observation.md` | Implemented | Show the cache keys plus fallback behavior to demonstrate Redis as a complement, not as the source of truth. |
| Indexes | Use indexes to enforce rules and support query access paths. | The schema contains operational indexes for active filters, foreign keys, timestamps, and partial unique rules. | `db/schema.sql` | Implemented | Add a short explanation of why the active and partial indexes matter during the presentation. |
| EXPLAIN / query performance | Prove query behavior with execution plans rather than intuition. | The repo has indexes and performance-oriented course references, but it does not store CampusEnroll-specific EXPLAIN plans yet. | `db/schema.sql`; `infra/k6/README.md`; no saved EXPLAIN artifacts were found under `docs/` or `infra/` | Documented | A small docs-only appendix with representative `EXPLAIN (ANALYZE, BUFFERS)` output would improve course alignment before presentation. |
| CPU vs I/O / BUFFERS | Interpret where query time is spent and whether plans are memory or disk heavy. | This topic exists in the course material, but not in current CampusEnroll evidence. | `viewer/files/PostgreSQL_Performance_Engineering.pdf` in the course ZIP; no CampusEnroll BUFFERS evidence in repo | Documented | Add a narrow performance note only if the team can gather real PostgreSQL plan evidence safely. |
| Partitioning | Split large tables for maintenance or hotspot control. | No PostgreSQL partitioned tables are present. | `db/schema.sql` | Future Production Upgrade | Keep partitioning out of the final implementation unless the dataset or write pattern actually demands it. |
| Anti-hotspot | Design keys and write paths to avoid concentration on one row or partition. | No anti-hotspot design is needed for the current small academic workload. | `db/schema.sql`; `infra/k6/load-50000-requests.js` | Future Production Upgrade | Mention it as an advanced scaling topic, not as a missing core feature for this delivery. |
| Types of databases | Understand where relational, cache, and messaging tools fit. | The repo demonstrates PostgreSQL plus Redis plus RabbitMQ, but it does not implement a broad comparison platform. | `docker-compose.yml`; `backend/course-service/README.md`; `backend/enrollment-service/README.md`; `backend/billing-service/README.md` | Partially Implemented | Present this as a pragmatic mix of transactional DB, cache, and message broker. |
| DCL / database security | Manage roles, grants, and least-privilege access. | The project uses simple local credentials and does not ship role/grant scripts. | `.env`; `db/schema.sql` | Documented | Add a docs-only security appendix or optional SQL example later if the team wants explicit course coverage without risky runtime change. |
| Stress tests | Validate behavior under load and controlled failure observation. | Load assets, concurrency assets, and a manual failure observation guide exist, but raw result artifacts are not stored in the repo. | `infra/k6/README.md`; `infra/k6/smoke-test.js`; `infra/k6/load-50000-requests.js`; `infra/k6/container-failure-observation.md` | Partially Implemented | Keep the distinction between test assets and actually captured runtime evidence. |
| k6 load testing | Run repeatable scripted load against the current HTTP surface. | The repo includes smoke, exact 50,000 request, and configurable load scenarios. | `infra/k6/load-50000-requests.js`; `infra/k6/smoke-test.js`; `infra/k6/README.md`; `README.md` | Implemented | For presentation, show the exact-load script and the previously reported pass result only as documented evidence unless you rerun it live. |
| Concurrent enrollment testing | Probe the most important duplicate-active rule under concurrent requests. | A dedicated k6 script targets the same student and section pair and expects `201` plus `409` behavior. | `infra/k6/concurrent-enrollment-test.js`; `infra/k6/README.md`; `backend/enrollment-service/src/test/java/com/campusenroll/enrollmentservice/enrollment/EnrollmentServiceTest.java` | Implemented | Use this as the clearest bridge between course concurrency theory and current project behavior. |
| Observability | Capture service health and metrics for runtime visibility. | Health checks, Prometheus metrics, and active Prometheus alert rules are real, but dashboards, notifications, and service-level governance remain incomplete. | `docker-compose.yml`; `docker-compose.apps.yml`; `infra/prometheus/prometheus.yml`; `infra/prometheus/rules/campusenroll-alerts.yml`; `README.md` | Partially Implemented | Present observability as a strong foundation with live rules, not as a finished production monitoring stack. |
| Prometheus | Scrape metrics from the running services. | Prometheus is configured and the five services expose `/actuator/prometheus`. | `infra/prometheus/prometheus.yml`; `backend/student-service/src/main/resources/application.yml`; `backend/course-service/src/main/resources/application.yml`; `backend/enrollment-service/src/main/resources/application.yml`; `backend/billing-service/src/main/resources/application.yml`; `backend/notification/src/main/resources/application.yml`; `backend/*/pom.xml` | Implemented | Show `/targets` and one service metrics endpoint to keep the evidence concrete. |
| Grafana | Visualize metrics and dashboards. | Grafana is present and accessible as infrastructure, but no real dashboards or alerting package is committed. | `docker-compose.yml`; `infra/grafana/README.md`; `infra/grafana/provisioning/README.md`; `README.md` | Partially Implemented | Do not claim finished dashboards; present Grafana as available infrastructure only. |
| SLOs | Define measurable service objectives and error budgets. | The repo now has a documented SLO set and matching alert thresholds, but no formal error-budget process or SLA contract. | `docs/course-audit/SLO_ALERTING_PROPOSAL.md`; `infra/prometheus/rules/campusenroll-alerts.yml` | Partially Implemented | Present the SLOs as academic operational targets, not as formal business commitments. |
| Alerting | Trigger action from metric thresholds or failures. | Prometheus now loads active alert rules for service down, missing targets, HTTP 5xx rate, and p95 latency. | `infra/prometheus/prometheus.yml`; `infra/prometheus/rules/campusenroll-alerts.yml`; `docker-compose.yml`; `README.md` | Implemented | Be explicit that alert evaluation is implemented in Prometheus, while Alertmanager routing and external notifications remain future work. |
| Backups | Create recoverable database snapshots. | PowerShell backup tooling is present and documented for PostgreSQL custom-format dumps. | `infra/backups/backup-postgres.ps1`; `infra/backups/README.md`; `infra/backups/DISASTER_RECOVERY_RUNBOOK.md`; `README.md` | Implemented | This is one of the strongest course-alignment areas and should be shown directly. |
| Restore | Recover a database from a known backup artifact. | A guarded restore script and verification script are included. | `infra/backups/restore-postgres.ps1`; `infra/backups/verify-database.ps1`; `infra/backups/DISASTER_RECOVERY_RUNBOOK.md`; `README.md` | Implemented | Keep restore clearly separated from failover in the presentation narrative. |
| RPO / RTO | Explain recovery point and recovery time objectives. | The disaster recovery runbook explains academic RPO and RTO in plain terms. | `infra/backups/DISASTER_RECOVERY_RUNBOOK.md` | Documented | Use the runbook wording and avoid pretending these are automated production commitments. |
| PostgreSQL replication | Replicate the database to another PostgreSQL node. | No replication topology or replication config exists in the repo. | `docker-compose.yml`; `docs/ha/APPLICATION_FAILOVER_SWITCHOVER_DEMO.md` | Future Production Upgrade | Mention replication only as future work required for true database HA. |
| Read replicas | Route selected reads to replica nodes. | No replica database nodes or read-routing logic exist. | `docker-compose.yml`; `backend/` | Future Production Upgrade | Keep this as roadmap material, not a promised feature for the final presentation. |
| Database failover | Automatically or operationally switch the database role on failure. | CampusEnroll does not implement PostgreSQL failover; recovery is backup plus restore. | `README.md`; `infra/ha/README.md`; `docs/ha/APPLICATION_FAILOVER_SWITCHOVER_DEMO.md`; `infra/backups/DISASTER_RECOVERY_RUNBOOK.md` | Future Production Upgrade | Repeat this distinction consistently so the team does not overclaim HA. |
| Switchover | Perform a planned traffic handoff for maintenance. | Switchover exists only at the application layer for `course-service` through HAProxy. | `infra/ha/switchover-demo.ps1`; `docs/ha/APPLICATION_FAILOVER_SWITCHOVER_DEMO.md`; `docker-compose.ha-demo.yml` | Partially Implemented | Say "application switchover" every time to avoid confusion with database switchover. |
| Docker | Package infrastructure and the app stack for repeatable local execution. | The project uses Compose layers for infra, app containers, and the HA demo overlay. | `docker-compose.yml`; `docker-compose.apps.yml`; `docker-compose.ha-demo.yml` | Implemented | This is strong presentation material because it makes the local platform shape explicit. |
| HAProxy application failover | Keep an application route available when the primary service instance stops. | HAProxy fronts `course-service` and `course-service-replica` with health checks and scripted demos. | `docker-compose.ha-demo.yml`; `infra/load-balancer/haproxy.cfg`; `infra/ha/failover-demo.ps1`; `docs/ha/APPLICATION_FAILOVER_SWITCHOVER_DEMO.md` | Implemented | This is implemented and demonstrable, but it covers only the catalog path and not the database layer. |
| Multiple nodes | Run more than one active service instance as part of availability strategy. | A second node exists only for `course-service` in the HA demo layer; the rest of the stack remains single-instance. | `docker-compose.ha-demo.yml`; `infra/load-balancer/haproxy.cfg`; `README.md` | Partially Implemented | Present this as a narrow replica demo, not as full multi-node platform HA. |
| Frontend scope | Decide whether UI work belongs in the deliverable. | Frontend is intentionally excluded; Postman is the active client path. | `README.md`; `postman/README.md`; `infra/backups/README.md` | Not Applicable by Scope | Keep the scope statement explicit so presentation time stays on database, services, and HA topics. |
